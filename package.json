{
  "name": "hub-token-pls",
  "version": "1.0.0",
  "description": "Generate and copy hub user token to clipboard",
  "main": "./client/index.js",
  "bin": {
    "token4": "./bin/index.js"
  },
  "preferGlobal": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^7.0.0",
    "cross-fetch": "^3.0.6"
  },
  "devDependencies": {
    "@esri/arcgis-rest-auth": "^3.0.0",
    "@esri/arcgis-rest-request": "^3.0.0",
    "dotenv-json": "^1.0.0",
    "isomorphic-form-data": "^2.0.0"
  }
}
